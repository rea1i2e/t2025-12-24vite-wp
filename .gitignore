# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Deploy secrets (local only)
.env.deploy

# CI 生成: ドキュメントルート向け Basic（.htaccess / .htpasswd）
staging/docroot/

# 動画圧縮（raw/videos を型録から手動複製したとき。素材・案件別設定はコミットしない）
raw/videos/*.mp4
raw/videos/*.webm
raw/videos/*.mov
raw/videos/*.avi
raw/videos/compress-config.json
raw/videos/compress-report.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# QA 比較用スクショ（ローカル生成のみ）
qa-screenshots/

# cross-tool-session-handoff: session-log（ローカルのみ。チーム非共有）
memo/session-log.md
